import React from "react";

const VideoConsultation = ({ title = "Video Consultation" }) => (
  <section className="metric-card">
    <h4>{title}</h4>
    <p>Video consultation area.</p>
  </section>
);

export default VideoConsultation;
